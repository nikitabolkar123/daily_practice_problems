# Evaluate a given expression
# Google Translate Icon
# Given an expression containing numeric operands with addition and subtraction operators, evaluate it. Assume that the
# expression is valid and may contain sub-expressions enclosed in opening and closing braces.
#
# def evaluate_expression(s):
#     stack = []
#     operand = 0
#     result = 0
#     sign = 1
#
#     for i in range(len(s)):
#         c = s[i]
#
#